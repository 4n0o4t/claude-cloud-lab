# plans/

阶段计划与主 checklist。

- 文件名：`YYYY-MM-DD-<slug>.md`，从 `../templates/plan.md` 复制。
- 同一时间只有一个主 checklist，路径写在 `../status.md` 的“当前任务”里。
- 跨多轮的 review checklist 也放这里，从 `../templates/review-checklist.md` 复制。
- 计划完成后保留文件作为记录，在 status 中移除指针即可。
